# Workspace Rules

- **Deployment Protocol:** NEVER run deployment commands (e.g., `npx vercel --prod`, `npm run deploy`) without asking for the user's explicit permission first. Always wait for the user to approve the deployment before executing it.
